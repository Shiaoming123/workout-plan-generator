# 训练计划生成器 | Workout Plan Generator

一个基于规则的 MVP 健身训练计划生成器，根据用户的目标、身体条件、场地器械等信息，自动生成量身定制的周/月/季度训练计划。

## 功能特性

- 📋 **多维度输入**：支持目标、经验、场地、器械、身体限制等多种参数
- 🎯 **智能生成**：基于规则模板自动生成个性化训练计划
- 📅 **多周期支持**：可生成周计划（1周）、月计划（4周）、季度计划（12周）
- 🔒 **安全优先**：根据身体限制自动避开高风险动作
- 💾 **多种导出**：支持复制文本、下载JSON、打印
- 🎨 **响应式设计**：适配桌面和移动端
- 🖨️ **打印友好**：优化的打印样式

## 技术栈

- **框架**: React 18 + TypeScript
- **构建工具**: Vite
- **样式**: Tailwind CSS
- **架构**: 纯前端，无后端依赖

## 快速开始

### 安装依赖

```bash
npm install
```

### 运行开发服务器

```bash
npm run dev
```

访问 `http://localhost:5173` 查看应用。

### 构建生产版本

```bash
npm run build
```

构建产物将生成在 `dist/` 目录。

### 预览生产版本

```bash
npm run preview
```

## 项目结构

```
workout-plan-generator/
├── src/
│   ├── components/          # React 组件
│   │   ├── Header.tsx       # 页面头部
│   │   ├── InputForm.tsx    # 输入表单
│   │   ├── PlanDisplay.tsx  # 计划展示
│   │   └── ExportButtons.tsx # 导出按钮
│   ├── data/                # 数据与模板
│   │   ├── exercises.ts     # 动作数据库
│   │   └── templates.ts     # 训练模板
│   ├── lib/                 # 核心逻辑
│   │   └── planGenerator.ts # 计划生成算法
│   ├── types/               # TypeScript 类型
│   │   └── index.ts
│   ├── utils/               # 工具函数
│   │   └── export.ts        # 导出功能
│   ├── App.tsx              # 主应用组件
│   ├── main.tsx             # 入口文件
│   └── index.css            # 全局样式
├── package.json
├── vite.config.ts
├── tsconfig.json
└── README.md
```

## 核心逻辑说明

### 1. 生成算法 (`src/lib/planGenerator.ts`)

**主函数**: `generatePlan(profile: UserProfile): TrainingPlan`

生成流程：
1. 根据 `period` 决定生成周/月/季度计划
2. 对每一周调用 `generateWeekPlan()` 并应用 `volumeMultiplier` 进行周期化
3. 对每一天调用 `generateWorkoutSession()` 生成具体训练
4. 每个训练包含 4 个阶段：热身 → 主训练 → 辅助 → 放松

**动作筛选逻辑**：
- 根据用户器械 (`equipment`) 筛选可用动作
- 根据身体限制 (`constraints`) 排除禁忌动作
- 根据目标 (`goal`) 决定力量/有氧/HIIT 占比

### 2. 动作数据库 (`src/data/exercises.ts`)

每个动作包含：
- `id`: 唯一标识
- `name` / `nameZh`: 英文/中文名称
- `category`: 分类（热身/力量/有氧/HIIT/拉伸等）
- `requiredEquipment`: 需要的器械
- `contraindications`: 禁忌症（哪些身体问题应避免）
- `isHighImpact`: 是否高冲击

**扩展动作库**：
在 `exercises.ts` 中对应的数组添加新动作即可：
```typescript
export const upperStrengthExercises: Exercise[] = [
  // 现有动作...
  {
    id: 'upper_new',
    name: 'New Exercise',
    nameZh: '新动作',
    category: 'strength_upper',
    requiredEquipment: ['dumbbells'],
    targetMuscles: ['胸', '三头'],
    difficulty: 'intermediate',
    contraindications: ['shoulder_issue'],
    isHighImpact: false,
  },
];
```

### 3. 训练模板 (`src/data/templates.ts`)

**目标模板** (`goalTemplates`):
定义不同目标的训练类型占比：
- `fat_loss`: 有氧40% + HIIT20% + 力量30%
- `muscle_gain`: 力量70% + 有氧10%
- `fitness`: 均衡配比
- `rehab`: 活动度50% + 低冲击

**经验修正器** (`experienceModifiers`):
- `beginner`: 容量70%，休息1.5倍
- `intermediate`: 标准容量
- `advanced`: 容量130%，休息0.8倍

**周期化进阶**:
- 月计划：Week 1-3 逐步增量，Week 4 减量（deload）
- 季度计划：Month 1-3 阶梯式增强

### 4. 约束规则 (Constraints)

在 `planGenerator.ts` 中的 `getAvailableExercises()` 函数实现：

- **膝盖不适** (`knee_issue`): 排除跳跃、深蹲跳等高冲击动作
- **腰背不适** (`back_issue`): 排除大重量硬拉、深屈髋动作
- **肩部不适** (`shoulder_issue`): 排除过头推举、宽握动作
- **产后恢复** (`postpartum`): 优先核心重建，低冲击
- **高血压** (`hypertension`): 避免憋气用力，中低强度

## 如何扩展

### 添加新的训练目标

1. 在 `src/types/index.ts` 的 `Goal` 类型中添加新目标：
```typescript
export type Goal = 'fat_loss' | 'muscle_gain' | 'fitness' | 'rehab' | 'general' | 'your_new_goal';
```

2. 在 `src/data/templates.ts` 中添加对应模板：
```typescript
export const goalTemplates: Record<string, GoalTemplate> = {
  your_new_goal: {
    goal: 'your_new_goal',
    strengthRatio: 0.5,
    cardioRatio: 0.3,
    hiitRatio: 0.1,
    mobilityRatio: 0.1,
    description: 'Your New Goal',
    descriptionZh: '你的新目标',
  },
};
```

3. 在 `src/components/InputForm.tsx` 中添加选项。

### 添加新的器械类型

1. 在 `src/types/index.ts` 的 `Equipment` 类型中添加
2. 在动作数据库中标注需要该器械的动作
3. 在表单中添加对应选项

### 自定义周期化策略

修改 `src/data/templates.ts` 中的：
- `monthlyProgression`: 调整 4 周的容量倍数
- `quarterlyProgression`: 调整 3 个月的进阶策略

### 调整生成逻辑

核心生成逻辑在 `src/lib/planGenerator.ts`：
- `generateMainWork()`: 修改主训练生成规则
- `createStrengthSet()`: 调整组数、次数、休息时间
- `selectRandom()`: 改为智能选择而非随机

## 单元测试建议

虽然 MVP 未包含测试，但推荐为以下模块编写测试：

1. **动作筛选逻辑**:
   - `filterByEquipment()`
   - `filterByConstraints()`
   - `filterHighImpact()`

2. **计划生成**:
   - `generatePlan()` 对不同 `period` 的输出结构
   - 验证周期化倍数正确应用

3. **导出功能**:
   - `formatPlanAsText()` 输出格式正确性

## 部署

### Vercel / Netlify
1. 连接 Git 仓库
2. 构建命令: `npm run build`
3. 输出目录: `dist`

### 静态服务器
```bash
npm run build
# 将 dist/ 目录内容部署到任意静态服务器
```

## 注意事项

⚠️ **免责声明**：
- 本工具仅供参考，不构成专业医疗或训练建议
- 开始任何训练计划前，请咨询医生或专业教练
- 训练中如有不适请立即停止

## 未来改进方向

- [ ] 集成 AI 大模型生成更智能的计划
- [ ] 添加训练进度跟踪功能
- [ ] 支持计划历史记录
- [ ] 视频示范集成
- [ ] 多语言支持
- [ ] 后端 API + 用户系统
- [ ] 社区分享功能

## 开源协议

MIT License

## 联系方式

如有问题或建议，欢迎提 Issue。

---

**Enjoy your personalized workout journey! 💪**
